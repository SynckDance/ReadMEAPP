export default function LibraryPage() {
  return (
    <section>
      <h1 className="text-3xl font-serif">Library</h1>
      <p className="mt-2 opacity-80">
        Your documents will live here. Upload PDF, EPUB, DOCX, paste text, or
        add a URL.
      </p>
      <div className="mt-8 border border-dashed border-rule p-10 text-center opacity-70">
        Phase 1 — ingest + reader.
      </div>
    </section>
  );
}
