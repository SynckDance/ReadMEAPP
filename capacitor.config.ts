import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.synckdance.readmeapp",
  appName: "ReadMEAPP",
  webDir: "out",
  ios: {
    contentInset: "always",
  },
};

export default config;
